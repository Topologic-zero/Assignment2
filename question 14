import json

    with open(schedule.json, "r") as file:
        return json.load(file)


    load_schedule_from_json(schedule.json)
    result = ""
    for event in events:
      print(  f"⏰ {event['time']}: {event['name']}\n")

