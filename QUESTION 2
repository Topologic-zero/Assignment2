def reverse_and_celebrate(words):
    result = ""
    for i in range(len(words)-1, -1, -1):
        result += words[i]
        if i != 0:  # add dash except after last
            result += "-"
    return result + "🎉"
print(reverse_and_celebrate(["Python", "is", "fun"]))
